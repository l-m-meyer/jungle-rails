<section class="admin-products-index">

  <%= tag.header class: "page-header text-center" do %>
    <%= tag.h1 do %>
      <%= tag.span "Admin »", class: "text-muted" %> New Product
    <% end %>
    <%= tag.div link_to 'New Product', [:new, :admin, :product], class: "btn btn-success my-4 fs-5" %>  
  <% end %>

  <div class="panel panel-default products">

    <table class="table table-bordered">
      <thead>
        <tr>
          <th colspan="2">Product</th>
          <th>Category</th>
          <th>Price</th>
          <th>Qty Remaining</th>
          <th></th>
        </tr>
      </thead>
      <tbody>
        <%= render @products %>
      </tbody>
    </table>

  </div>

</section>
